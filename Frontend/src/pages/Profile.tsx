import Layout from "../Layout";
import ProfileForm from "../components/forms/ProfileForm";

const Profile = () => {
  return (
    <Layout>
      <ProfileForm />
    </Layout>
  );
};

export default Profile;