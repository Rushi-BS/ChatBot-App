import Layout from "../Layout";
import ChatsList from "../components/ChatsList";

const Home: React.FC = () => {
  return (
    <Layout>
      <ChatsList />
    </Layout>
  );
};

export default Home;
