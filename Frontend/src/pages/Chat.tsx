import ChatWindow from "../components/ChatWindow";
import Layout from "../Layout";

const Chat: React.FC = () => {
  return (
    <Layout>
      <ChatWindow />
    </Layout>
  );
};

export default Chat;