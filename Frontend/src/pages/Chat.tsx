import ChatWindow from "../components/ChatWindow";
import Layout from "../utils/Layout";

const Chat: React.FC = () => {
  return (
    <Layout>
      <ChatWindow />
    </Layout>
  );
};

export default Chat;